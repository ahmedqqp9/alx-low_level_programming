#include "main.h"

/**
 * print_alphabet_x10 -function that will print the  alphabe 10 times
 *
  */

void print_alphabet_x10(void)
{
	int line. ch;
	
	for (line = 0; line <= 'z'; ch++)
	{	

	        for (ch = 'a'; cd <= 'z' ; ch++)	
			_putchar(ch);
		_putchar('\n');
        }
}
